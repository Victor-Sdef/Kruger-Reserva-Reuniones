package com.kruger.kevaluacion.entity;

/**
 * Enum que define los roles de usuario permitidos por el sistema.
 * Solo ADMIN puede crear usuarios, según el documento.
 */
public enum Role {
    ADMIN,
    USER
}
